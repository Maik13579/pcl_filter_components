# pcl_filter_tests

`pcl_filter_tests` contains installed-system integration tests that require the
whole split package set to be present.

Coverage includes:

- filter and type adapter export discovery through the ament index
- `rclcpp_components` registration metadata
- installed factory example pipeline validation
